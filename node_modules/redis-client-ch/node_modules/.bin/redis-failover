#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../node-redis-failover/bin/redis-failover" "$@"
  ret=$?
else 
  node  "$basedir/../node-redis-failover/bin/redis-failover" "$@"
  ret=$?
fi
exit $ret
